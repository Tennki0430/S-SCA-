# 作業ログ 2026-04-27

## やったこと

### ハーネス設計への移行（大規模リファクタリング）
- PDCA サイクルを明示した harness 設計を導入
  - `evaluators/` — C（Check）: 採点ロジック（accuracy.py / llm_judge.py / base.py）
  - `harness/` — 運営（runner.py / reporter.py / dataloader.py）
  - `config/settings.yaml` — 銘柄・モデル・閾値の設定を一元管理
  - `agents/prompts/` — Claude へのプロンプト関数（merchant.py / reflection.py）
- `accuracy_monitor.py` / `self_reflection.py` を削除し `evaluators/` に統合

### agents/ フォルダ構造の整備
- `agents/skills/` — エージェントの能力（discord_post.py / claude_call.py）
- `agents/rules/` — 守るべき制約（cost_policy.md / data_quality.md）
- `agents/hooks/` — イベント連携（on_evaluation_fail.py / on_oracle_complete.py）

### .claude/ の整理
- `.claude/agents/` に evaluators の定義を追加（accuracy_evaluator.md / llm_judge.md）
- `.claude/agents/scout_price.md` を5銘柄に更新
- `.claude/instructions.md` を削除（.claude/CLAUDE.md に統合）
- `.claude/skills/cost-management.md` を削除（agents/rules/ と重複）
- `.claude/CLAUDE.md` にトラブルシューティング早見表を追加

### ドキュメント整備
- `README.md` を現在の構成に全面更新
  - PDCA エージェント構成表を追加
  - ディレクトリ構造を harness/・evaluators/・agents/ 含む現行構成に更新
  - 地政学リスク指標・5銘柄を反映
- `CLAUDE.md`（ルート）の削除済みエージェントのコマンドを除去

### テストファイル整理
- `tests/test_accuracy_monitor.py` / `tests/test_self_reflection.py` を削除（削除済みモジュール参照で実行不可）

### GitHub コラボレーター設定
- 他のメンバーへの権限付与方法を確認
  - Settings → Collaborators → Add people

### Google Sheets 連携（GAS）
- Supabase の3テーブル（market_data / prediction_log / feedback_log）を Google スプレッドシートに同期する GAS スクリプトを作成
- スクリプトプロパティ（SUPABASE_URL / SUPABASE_KEY）から安全に認証情報を読み込む実装

## 次回に持ち越し
- GAS の `syncAll` 実行結果を確認する
- GAS のトリガーを1時間ごとに設定する
- Oracle 予測・Discord 通知が正常に届いているか確認する
- X API Secrets の登録（後回し）
- アフィリエイト投稿機能の実装（予測が安定してから）
